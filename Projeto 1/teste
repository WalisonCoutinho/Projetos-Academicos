#include <stdio.h>
int main() {
    int valor;
    printf("Digite um valor:");
    scanf("%d",&valor);

    if (valor > 0 && valor < 100){
        printf("O %d e valor  valido!",valor);
    }else {
        printf("O %d e valor invaido",valor);
    }
return 0;

}